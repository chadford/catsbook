pull_request_rules:
  - name: Automatic merge for passing scala-steward PRs
    conditions:
      - or:
        - author=chadford
        - head~=^update/.?$
      - base=master
      - check-success=build
    actions:
      merge:
        method: squash
